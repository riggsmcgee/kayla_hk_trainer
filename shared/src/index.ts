/**
 * @dojo/shared public surface. Types only — no runtime exports.
 *
 * The `.js` extension keeps this import valid under both `bundler` and
 * `NodeNext` module resolution, whichever a consuming workspace uses.
 */
export type { PracticeMode, EnemyId, PracticeRun, SettingsV1 } from './types.js';
