# Dependencies
node_modules/

# Build output
dist/
coverage/

# Disposable practice-server database (localStorage is the source of truth)
server/data/
*.db

# Environment files
.env
.env.*

# OS cruft
.DS_Store
Thumbs.db

# NOTE: .claude/, .agents/, and skills-lock.json are intentionally tracked
# (project-local agent skills live there). Do not add ignore rules for them.
